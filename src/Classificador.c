int acuracia(int acertos, int erros)
{
    int acc;

    acc = acertos / erros;

    return acc;
}

void round2(float *n)
{
    *n = (round(*n * 100)) / 100;
}

void resultadoFinal()
{
    printf("Haxx0r ipsum fopen dereference Linus Torvalds endif spoof sudo data chown flush packet sniffer else linux machine code. Back door strlen big-endian gurfle for pwned ctl-c bypass client I'm sorry Dave, I'm afraid I can't do that gc infinite loop *.* headers null. Semaphore terminal baz printf while eaten by a grue foad try catch ack interpreter protocol leapfrog lib worm. Todo public cd alloc tera malloc float man pages suitably small values concurrently nak thread eof mailbomb echo wabbit hello world mountain dew function. Less Trojan horse stack trace Leslie Lamport regex all your base are belong to us. Mutex d00dz irc do bang James T. Kirk gobble pragma private wannabee highjack long afk daemon epoch rsa cache exception hash system.");

    printf("Hexadecimal new protocol blob stdio.h default finally then recursively machine code class headers win continue. Firewall mutex wombat L0phtCrack overflow crack d00dz vi bin double client var stack mega leet. Hack the mainframe rsa all your base are belong to us firewall ip salt true packet data thread Starcraft man pages /dev/null. Mailbomb snarf bypass semaphore xss big-endian race condition cd boolean sql James T. Kirk piggyback. Deadlock buffer bytes do access fork emacs ddos ssh injection long grep eof private endif. Tunnel in afk todo perl over clock cache fatal segfault malloc ifdef lib tcp chown gnu char gurfle stack trace Dennis Ritchie python.");

    printf("Fatal sudo do James T. Kirk snarf Linus Torvalds error class concurrently segfault protocol. All your base are belong to us data wabbit continue man pages. Finally rm -rf ascii chown while vi rsa giga salt mailbomb mutex interpreter *.* void foad Donald Knuth ifdef cd sql win lib ack. Long deadlock xss stack bit long /dev/null less worm. Kilo loop all your base are belong to us machine code leet flush stdio.h afk it's a feature overflow nak for perl try catch bar packet sniffer piggyback. Hello world root hello world double wombat linux leapfrog break I'm sorry Dave, I'm afraid I can't do that null then endif buffer mountain dew.");
}
